<div class="nav-bar clearfix">
  <div class="third">
    <div class="home relative-center-y">
      {{#link-to "dashboard" session.authenticatedUser}}<img class="icon" src="assets/images/icon-home.png">{{/link-to}}
      {{#link-to "dashboard" session.authenticatedUser class="title dark-gray-link"}}Home{{/link-to}}
    </div>
  </div>

  <div class="third">
    <img class="relative-center-xy" src="assets/images/telegram-logo-small.png">
  </div>

  <div class="third">
    <div class="user relative-center-y">
      {{#link-to "user" session.authenticatedUser class="avatar link"}}<img src="{{authenticatedUser.photo}}"/>{{/link-to}}
      {{#link-to "user" session.authenticatedUser class="name dark-gray-link"}}{{authenticatedUser.name}}{{/link-to}}
      <a class="logout dark-gray-link" {{action "sendLogOut"}}>Log out</a>
    </div>
  </div>
</div>
